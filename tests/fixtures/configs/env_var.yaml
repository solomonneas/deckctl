version: 1
default_profile: streaming
obs_hosts:
  studio:
    url: obsws://192.0.2.10:4455/${DECKCTL_TEST_OBS_PASS}
profiles:
  streaming:
    default_page: home
    pages:
      home:
        keys:
          0:
            icon: {text: "Cam"}
            action: {type: obs.scene.switch, host: studio, scene: "Camera"}
